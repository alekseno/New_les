import time
import pprint
import math


pprint.pprint(dir(time)) # dir - посмотреть содержимое модуля
# pprint - вывод позиций на отдельной строке (без него- всё в одну строку)
print("--------следующий модуль---------")
pprint.pprint(dir(math))
